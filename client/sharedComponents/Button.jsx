import React, { useEffect, useState } from 'react';

const Button = ({children, onClick, ...props}) => {
    
    // on click action
    // styles
        // size - use padding to determine size
        // color
        // shape
        // centered text with equal right and more right/left padding
    // put class on link
    // button text

}

const StyledButton = styled.button``